
## sbt project

Create sbt project like normal in a build directory.

In sbt project, add shared.jar with a symlink: ln -s ~/.config/augustmc/shared.jar .

In build.sbt add line: unmanagedJars in Compile += file("shared.jar")

## intellij

Import sbt project.

Set build automatically and import automatically.


